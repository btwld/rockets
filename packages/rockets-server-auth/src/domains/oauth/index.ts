// OAuth Domain Public API
//
// TODO(upstream: concepta/nestjs-auth-apple|github|google) — re-export
// `AuthOAuthController` once v8 OAuth provider packages ship. The
// controller body is preserved (commented) in
// `./controllers/auth-oauth.controller.ts` for fast restore.

export {};
