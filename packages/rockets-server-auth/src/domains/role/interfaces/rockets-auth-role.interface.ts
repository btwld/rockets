import { RoleInterface } from '@concepta/nestjs-role';

/**
 * Rockets Server Role Interface
 *
 * Extends the base role interface from the common module
 */
export interface RocketsAuthRoleInterface extends RoleInterface {}
