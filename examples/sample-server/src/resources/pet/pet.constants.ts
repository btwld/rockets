/**
 * Repository / resource key for the pet entity. Single source of truth
 * for `@InjectDynamicRepository(...)`, `defineResource({ key })`, and
 * `repositories.entities` registration.
 */
export const PET_ENTITY_KEY = 'pet';
